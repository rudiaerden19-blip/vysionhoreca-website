import { sectorLandingMetadata } from '@/lib/sector-landings'

export const metadata = sectorLandingMetadata('nachtwinkel')

export default function NachtwinkelSectorLayout({ children }: { children: React.ReactNode }) {
  return children
}
