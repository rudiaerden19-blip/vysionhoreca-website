import { sectorLandingMetadata } from '@/lib/sector-landings'

export const metadata = sectorLandingMetadata('bakkerij')

export default function BakkerijSectorLayout({ children }: { children: React.ReactNode }) {
  return children
}
