import { sectorLandingMetadata } from '@/lib/sector-landings'

export const metadata = sectorLandingMetadata('slagerij')

export default function SlagerijSectorLayout({ children }: { children: React.ReactNode }) {
  return children
}
