import { sectorLandingMetadata } from '@/lib/sector-landings'

export const metadata = sectorLandingMetadata('frituur')

export default function FrituurSectorLayout({ children }: { children: React.ReactNode }) {
  return children
}
